const a = 200; // a constante "a" teve uma atribuição de 200 antes do proximo "a".

a = 100; // 

console.log(a); //estoura um erro, porque se você declara um numero à uma constante, ela grava apenas aquele numero e nenhum outro. O certo seria mudar pra variável(var) se você quissesse fazer uma alteração depois.