const listaCompras = ["Maçã", "Banana", "Laranja", "Pão", "Alface"]; // constante de uma "lista de compras"

var i = 0; // variavel i é igual a 0.

while(i < listaCompras.length){  // enquanto i for menor que a quantidade da lista
     console.log(listaCompras[i]); // o log mostrará e reenviará a lista até a quinta vez
     i++; // seguindo até chegar o ultimo item. Tenha cuidado pois ao usar isso você fará o number ou seja lá o que for continuar aumentando.
}
